# SECRETS ONLY. All vars are prefixed TEG_. Copy to .env (gitignored) and fill in.
# Non-secret config lives in config/settings.yaml (committed) and
# config/settings.local.yaml (gitignored). Do not put endpoints/ids/model names here.

TEG_JIRA_TOKEN=
TEG_SEARCH_API_KEY=
TEG_COSMOS_KEY=
TEG_IDP_CLIENT_SECRET=
TEG_IDP_PASSWORD=
TEG_AZURE_CLIENT_SECRET=
